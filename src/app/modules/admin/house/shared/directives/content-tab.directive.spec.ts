import { ContentTabDirective } from './content-tab.directive';

describe('ContentTabDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentTabDirective();
    expect(directive).toBeTruthy();
  });
});
